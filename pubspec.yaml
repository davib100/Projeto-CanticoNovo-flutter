name: myapp_project
description: "A Flutter project containing multiple modules."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.28.2
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.0
  path: ^1.8.3
  connectivity_plus: ^6.0.3
  rxdart: ^0.28.0
  get_it: ^7.7.0
  sentry_flutter: ^9.7.0
  package_info_plus: ^9.0.0
  device_info_plus: ^12.1.0
  flutter_secure_storage: ^9.2.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.28.0
  build_runner: ^2.4.7
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
